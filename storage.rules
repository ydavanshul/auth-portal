rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // By default restrict all access, forcing backend-only overrides
      allow read, write: if false;
    }
  }
}
